-- 야간(21시~익일 8시) 광고성 정보 수신 동의.
--
-- ── 왜 별도 항목인가 ────────────────────────────────────────────────
-- 「정보통신망법」 제50조 3항은 오후 9시부터 다음 날 오전 8시까지 광고성
-- 정보를 전송하려면 **별도의 사전 동의** 를 받도록 합니다. 일반 광고 수신
-- 동의로는 그 시간대에 보낼 수 없습니다.
--
-- 펀딩 오픈이 자정이면 그 시각에 알릴 수 없습니다. 지금 받아 두지 않으면
-- 2027년 2월에 가장 중요한 알림을 못 보내거나, 동의 없이 보내게 됩니다.
--
-- ── 왜 시각인가 ────────────────────────────────────────────────────
-- consented_at·marketing_at 과 같은 이유입니다. "동의했다" 만 남기면 언제
-- 받았는지 증명할 수 없습니다. NULL 이 미동의입니다.
--
-- **선택 항목입니다.** 기본값은 없고, 화면에서도 체크가 풀린 상태로 시작합니다.
-- 필수로 만들면 그건 동의가 아닙니다.
ALTER TABLE launch_notify ADD COLUMN night_at TEXT;
ALTER TABLE panel_applications ADD COLUMN night_at TEXT;

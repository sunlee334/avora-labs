/**
 * 처방 평가에 **바깥 사람을 모집하는가.**
 *
 * ── 왜 꺼져 있나 ───────────────────────────────────────────
 * 한동안 사이트는 러닝 크루·클라이밍짐에서 검증단을 모으고, 그분들이 점수를
 * 매긴다고 말했습니다. 그런데 제품 기획안 v09 와 사업 기획서 v04 어디에도
 * **「검증단」·「블라인드」·「집계」가 한 번도 나오지 않습니다.**
 *
 * 기획안 5-5 가 적은 실제 방법은 이렇습니다.
 *
 *   평가는 2명이 각각 독립적으로 진행하고 이후 결과를 비교함.
 *   함께 보면서 평가하면 의견이 수렴되어 차이가 드러나지 않음.
 *   가능하면 제조사명을 가린 상태로 평가함.
 *
 * 기준을 먼저 정해 공개하는 것도, 제조사를 가리는 것도, 커트라인 미달을
 * 탈락시키는 것도 전부 사실입니다. **사실이 아닌 것은 「누가」 뿐이었습니다.**
 *
 * ── 왜 지우지 않고 껐나 ────────────────────────────────────
 * 모집을 다시 하기로 하면 폼·API·메일·처리방침 항목이 그대로 필요합니다.
 * 지우면 다시 만들어야 하고, 만드는 사이에 서로 어긋납니다. 여기 한 줄이
 * 다섯 곳을 함께 여닫습니다 — `notice.enabled` 와 같은 방식입니다.
 *
 *   /panel 의 지원 폼 섹션
 *   POST /api/panel
 *   개인정보처리방침의 지원자 수집 항목
 *   지원 접수 메일
 *   폼을 검사하는 e2e
 *
 * 켤 때는 이 값만 `true` 로 바꾸면 됩니다. 다만 켜기 전에 **기획안이 그
 * 방법을 담고 있는지** 먼저 확인하세요. 이 파일이 생긴 이유가 그것입니다.
 */
export const PANEL_APPLICATIONS_OPEN = false;
